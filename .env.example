# ============================================
# OpenAI API Configuration
# ============================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# ============================================
# Database Configuration
# ============================================
# For local development (SQLite)
DATABASE_URL=sqlite:///./data/second_guess.db

# For Docker deployment (PostgreSQL)
# DATABASE_URL=postgresql://langfuse:langfuse_password@postgres:5432/second_guess

# ============================================
# PostgreSQL Configuration (Docker only)
# ============================================
POSTGRES_USER=langfuse
POSTGRES_PASSWORD=langfuse_password
POSTGRES_DB=langfuse
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ============================================
# Langfuse Observability (Optional)
# ============================================
# Get these keys from Langfuse dashboard after creating a project
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here

# For local development with Docker
LANGFUSE_HOST=http://localhost:3000

# For Docker internal communication
# LANGFUSE_HOST=http://langfuse:3000

# For cloud-hosted Langfuse
# LANGFUSE_HOST=https://cloud.langfuse.com

# ============================================
# Langfuse Configuration (Docker only)
# ============================================
# Secret for NextAuth - generate a random string
# Example: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-change-this-in-production

# Salt for Langfuse - generate a random string
# Example: openssl rand -base64 32
SALT=your-salt-change-this-in-production

# Telemetry
TELEMETRY_ENABLED=true

# Experimental features
LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES=false
